import { EmailRegistroPipe } from './email-registro.pipe';

describe('EmailRegistroPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailRegistroPipe();
    expect(pipe).toBeTruthy();
  });
});
