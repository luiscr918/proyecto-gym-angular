import { FechaActualPipe } from './fecha-actual.pipe';

describe('FechaActualPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaActualPipe();
    expect(pipe).toBeTruthy();
  });
});
